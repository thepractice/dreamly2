<% provide(:title, 'Users') %>
<h1>Users</h1>

<%= will_paginate @users, renderer: BootstrapPagination::Rails %>

<ul class="users">
	<%= render @users %>
</ul>

<%= will_paginate @users, renderer: BootstrapPagination::Rails %>